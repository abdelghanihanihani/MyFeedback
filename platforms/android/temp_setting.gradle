android {
            defaultConfig {
                applicationId = "org.nativescript.MyFeedback"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }